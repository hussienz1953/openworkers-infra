# Context: server

location = /health {
  access_log off;

  add_header X-Matching-Path  health  always;
  add_header X-Request-Id     $rid    always;
  add_header X-Server-Id      $sid    always;

  return 200 "ok";
}
